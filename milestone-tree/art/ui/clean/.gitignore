# Lossless PNG stills and clip frames are regenerated by render.js / clip.js; the JPEG + MP4 previews in out/ are
# committed (the parent art/ui/.gitignore ignores every out/ directory, so re-include this one).
!out/
out/*.png
out/frames/
