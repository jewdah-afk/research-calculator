# Build outputs: the Blender renders (out/3d), the painted pieces (out/pieces), the realm backgrounds (out/bg) and the
# lossless sample renders are regenerated by blender/build.py, build.js and samples/render.js. What is committed:
# the sources, ui.json, the fonts, sfx/ and the JPEG / MP4 previews in previews/.
out/
blender/__pycache__/
*.blend
*.blend1
